import { css } from '@emotion/react';

export const styles = {
  title: css({
    padding: '1rem',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    h2: {
      fontSize: '1.6rem',
      fontWeight: 600,
    },
  }),
};

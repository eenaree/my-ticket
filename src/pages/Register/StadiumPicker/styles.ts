import { css } from '@emotion/react';

export const styles = {
  wrapper: css({
    padding: '1.25rem',
  }),
  title: css({
    h2: {
      paddingBottom: '1rem',
      fontSize: '1.6rem',
      fontWeight: 600,
    },
    p: {
      paddingBottom: '2rem',
    },
  }),
};
